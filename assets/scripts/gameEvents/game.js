'use strict'

/*const gameVariants = require('../lib/a.js')

const boggle = boggleVariants.boggle
const bigBoggle = boggleVariants.bigBoggle
const superBigBoggle = boggleVariants.superBigBoggle

const boggleTray = 'wxxxxoxxxxrxxxxd'.split('')

console.log(boggle(boggleTray, [[0, 0], [1, 1], [2, 2], [3, 3]])) */

const gameArray = []

let player_x = {
  username1,
  password,
  no_games
}

let player_o = {
  username2,
  password,
  no_games

let gameStats = {
  totalGames,
  totalWins
}

module.exports = {
  gameArray,
  player_x,
  player_o,
  gameStats
}
